export function Full() {
  // useForm
    // fields
      // optional field
      // required field
      // custom validation
      // validation array
      // validation using form
      // validation using context
      // object field (with and without validation)
      // array field (with and without validation)
    // validate
    // initial values
    // handleSubmit

  // useFieldValue (machinery/FormValues)

  // useField (machinery/FormTextInput) - note to self: check if we use all state props
  // useNumberField (machinery/FormNumberInput)
  // useArrayField (machinery/FormArrayField)

  // (machinery/FormObjectField)

  // counter (to show form values are preserved)
}
