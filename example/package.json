{
  "dependencies": {
    "@kaliber/build": "^3.1.0",
    "@kaliber/serve": "^1.0.5"
  },
  "scripts": {
    "start": "npm-run-all --parallel watch serve:dev",
    "watch": "CONFIG_ENV=dev NODE_OPTIONS='--preserve-symlinks' kaliber-watch",
    "serve:dev": "PORT=8000 CONFIG_ENV=dev NODE_OPTIONS='--preserve-symlinks' kaliber-serve"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "npm-run-all": "^4.1.5"
  }
}
